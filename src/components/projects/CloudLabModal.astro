---
import CloudLabPanel from "../../islands/CloudLabPanel";

const API_URL = "https://app-mylab-dev-api.azurewebsites.net/api/health";
const ARCH_IMG = "/images/Diagrams/Cloud Diagram.svg";
---

<section class="wrap">
  <header class="head">
    <h3 class="title">CloudLab — Azure App Service API</h3>
    <span class="muted">Express on Azure · Terraform · Secure App Settings</span
    >
  </header>

  <!-- Interaction (match BitLocker/AutoForge) -->
  <div class="section">
    <h4>Interaction Window</h4>
    <!-- ⬇️ No extra terminal-bar here; CloudLabPanel draws its own header -->
    <CloudLabPanel apiUrl={API_URL} client:visible />
  </div>

  <div class="section">
    <h4>Project Summary</h4>
    <ul class="bullets">
      <li>
        <strong>Backend:</strong> Node.js <em>Express</em> API with routes for
        <code>/api/health</code>, <code>/api/version</code>, and telemetry
        endpoints for portfolio data.
      </li>
      <li>
        <strong>Hosting:</strong> Azure App Service (Windows) managed through an
        App Service Plan. Environment variables define SQL connections and app secrets.
      </li>
      <li>
        <strong>Security:</strong> CORS allowlist + API key header for sensitive
        routes. App settings stored in Azure’s configuration, never in code.
      </li>
      <li>
        <strong>Infrastructure:</strong> Deployed via <em
          >Terraform (AzureRM)</em
        > — resource groups, plans, and apps are declared as code for full reproducibility.
      </li>
      <li>
        <strong>Monitoring &amp; Maintenance:</strong> Azure Kudu for console access
        and live log streaming with <code>az webapp log tail</code>.
      </li>
    </ul>
  </div>

  <figure class="figure">
    <img
      src={ARCH_IMG}
      alt="Azure App Service architecture for CloudLab API"
      loading="lazy"
    />
    <figcaption class="muted">
      Azure App Service hosting the Node.js Express API with Terraform-defined
      infrastructure.
    </figcaption>
  </figure>
</section>

<style>
  /* === Unified Neon Glass Design (matches BitLocker & AutoForge) === */
  .wrap {
    display: grid;
    gap: 18px;
    padding: 22px;
    border-radius: 18px;
    background: radial-gradient(
      120% 120% at 0% 0%,
      rgba(0, 40, 48, 0.35) 0%,
      rgba(0, 14, 18, 0.55) 60%,
      rgba(0, 10, 14, 0.7) 100%
    );
    border: 1px solid rgba(0, 255, 255, 0.18);
    box-shadow:
      0 0 35px rgba(0, 255, 255, 0.1),
      inset 0 0 1px rgba(255, 255, 255, 0.05);
  }

  /* Header */
  .head {
    display: flex;
    align-items: baseline;
    gap: 10px;
  }
  .title {
    font-family: Orbitron, system-ui, sans-serif;
    letter-spacing: 0.06em;
    font-size: 1.05rem;
    color: #9bdcff;
    text-shadow: 0 0 10px rgba(0, 200, 255, 0.25);
    margin: 0;
  }
  .muted {
    color: rgba(200, 230, 240, 0.75);
  }
  .hint {
    margin-top: -4px;
  }

  /* Section headings (same as other modals) */
  .section h4 {
    font-family: Orbitron, system-ui, sans-serif;
    color: #9bdcff;
    text-shadow: 0 0 10px rgba(0, 200, 255, 0.25);
    letter-spacing: 0.06em;
    font-weight: 600;
    margin: 8px 0 6px;
  }

  /* Body text */
  .section p,
  .section li {
    font-family: "JetBrains Mono", monospace;
    color: rgba(210, 240, 250, 0.9);
    font-size: 13px;
    line-height: 1.6;
  }
  .bullets {
    margin: 0;
    padding-left: 18px;
  }

  /* Inline code blocks */
  code {
    background: rgba(0, 255, 255, 0.05);
    border: 1px solid rgba(0, 255, 255, 0.15);
    border-radius: 6px;
    padding: 2px 6px;
    font-size: 12px;
    color: #b7ecff;
  }

  /* Figure styling */
  .figure img {
    width: 100%;
    height: auto;
    border-radius: 12px;
    border: 1px solid rgba(255, 255, 255, 0.06);
    box-shadow: 0 0 24px rgba(0, 229, 255, 0.08);
  }
  .figure figcaption {
    margin-top: 6px;
    font-size: 0.9rem;
    color: rgba(200, 230, 240, 0.75);
  }
</style>
